apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  priorityClassName: my-pc
  containers: 
  - name: app
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: 1
      limits:
        memory: "128Mi"
        cpu: 1